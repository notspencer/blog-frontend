import React from "react";

const Navbar = () => {
  return (
    <header className="relative bg-blue-600 text-white">
      {/* Navigation Bar */}
      <nav className="container mx-auto flex justify-between items-center py-5 px-6"></nav>
    </header>
  );
};

export default Navbar;
